// TODO: Implement Socket.io service
// Your teammates will implement this with Socket.io client
class SocketService {
  connect(token) {}
  disconnect() {}
  joinRoom(roomId) {}
  leaveRoom(roomId) {}
  on(event, callback) {}
  emit(event, data) {}
}

export default new SocketService();
